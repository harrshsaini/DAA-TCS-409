#include<stdio.h>
struct node{
    int info;
    struct node *ptr;
};
void concept(struct node *p, int n, int l, int key)
{
    for(int l=0;l=t;l++)
{
    for{}
    {
        for(int i=0,i=n-1,i++)
        {
            if(key=a[i])
            {
                printf("element found");
                break;
            }
            printf("element not found");
        }
    }
}
}
int main
{
    int a;
    int b;
    int c;
    int element;
    concept(element, a, b, c);
}
